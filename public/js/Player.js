// Generated by CoffeeScript 1.9.1
var Player,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Player = (function(superClass) {
  extend(Player, superClass);

  function Player(el, self, gm, events) {
    this.el = el;
    this.self = self;
    this.gm = gm;
    this.events = events;
    this.defaultSpeed = {
      x: 0,
      y: 400
    };
    Player.__super__.constructor.call(this, this.el, this.gm);
    if (this.self != null) {
      this.x(20);
      this.color("green");
    } else {
      this.x(pongScreen.width - this.width() - 20);
      this.color("red");
    }
  }

  Player.prototype.moveUp = function() {
    this.currentSpeed.y = this.speed.y;
    return this.move(this.currentSpeed);
  };

  Player.prototype.moveDown = function() {
    this.currentSpeed.y = -this.speed.y;
    return this.move(this.currentSpeed);
  };

  Player.prototype._update = function() {
    if (this.self != null) {
      if (!this.events.events[this.events.keyCodes.w] && !this.events.events[this.events.keyCodes.s]) {
        return this.currentSpeed.y = 0;
      } else {
        if (this.events.events[this.events.keyCodes.w]) {
          this.moveUp();
        }
        if (this.events.events[this.events.keyCodes.s]) {
          return this.moveDown();
        }
      }
    } else {
      if (this.events.events.other[this.events.keyCodes.w]) {
        this.moveUp();
      }
      if (this.events.events.other[this.events.keyCodes.s]) {
        return this.moveDown();
      }
    }
  };

  Player.prototype.restart = function() {
    this.restartY();
    this.speed = {
      x: this.defaultSpeed.x,
      y: this.defaultSpeed.y
    };
    return this.currentSpeed = {
      x: 0,
      y: 0
    };
  };

  return Player;

})(Object);
